import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textLabel: {
    fontWeight: 'bold',
    fontSize: 17,
    lineHeight: 20,
    color: '#828282'
  },
  textValue: {
    fontWeight: 'bold',
    fontSize: 17,
    lineHeight: 20,
    color: '#333333'
  }
});

export default styles;