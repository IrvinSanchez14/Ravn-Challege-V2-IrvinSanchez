import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    backgroundColor: '#fff'
  },
  errorText: {
    textAlign: 'center',
    color: '#EC5757',
    fontSize: 17,
    lineHeight: 20,
    fontWeight: 'bold',
    marginTop: 16
  }
});

export default styles;